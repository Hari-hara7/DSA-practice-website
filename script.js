
        // JavaScript function for starting challenges
        function startChallenge(challengeLink) {
            // Redirect to the specified challenge link
            window.location.href = challengeLink;
        }
